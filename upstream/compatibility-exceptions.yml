# Paths, symbols, and symbol hashes in each populated `upstream` block are
# exact historical source identifiers. A changed symbol hash invalidates the
# exception until it is reviewed again. Product-owned names in this port are
# InvisibleDragon and SimpleDragon.
- id: deterministic-auto-id
  upstream:
    path: src/epsimple/core/shape.py
    symbol: Surface.__init__
    symbol_hash: sha256:bd742aa0e5cee63e774cb93eb9bb81267178cda8632b06462f3edc3a7621fe68
  difference:
    upstream: Python memory-address-based automatic identifiers
    dotnet: deterministic persistent identifiers
  effect:
    engineering_result: none
    raw_idf_text: generated names can differ
  approval: accepted

- id: immutable-conversion
  upstream:
    path: src/epsimple/core/model.py
    symbol: GreenRetrofitModel._dragonize_surface
    symbol_hash: sha256:8c531a98c322f6cde72eb6ce511159f5ac3f001098519e03dd2b14213fa4546a
  difference:
    upstream: temporarily mutates unknown constructions and resets them
    dotnet: resolves constructions on immutable copies
  effect:
    engineering_result: none
  approval: accepted

- id: rhino-7-support
  upstream:
    path: null
    symbol: null
    symbol_hash: null
  difference:
    upstream: no Rhino or Grasshopper plugin
    dotnet: multi-targets Rhino 7 (.NET Framework 4.8) and Rhino 8 (.NET 8)
  effect:
    engineering_result: none
    packaging: separate runtime-specific plugin payloads
  approval: user-requested

- id: legacy-people-activity-type-limit
  upstream:
    path: src/idragon/dragon/model.py
    symbol: EnergyModel.create_default_idf
    symbol_hash: sha256:585b53682bd5dbd4d2081e79eddc2789fa60925baafb5eae26de0541346ac9f4
  difference:
    upstream: emits the people-activity Schedule:Constant reference as Real although the declared object is named ScheduleTypeLimits:Real
    dotnet: preserves that authoring text exactly and normalizes the alias only in a disposable validation copy
  effect:
    engineering_result: none
    raw_idf_text: exact upstream text retained
    energyplus: same not-validated warning as pinned Python 0.7.0
    grasshopper_validation: warning instead of a blocking error
  approval: accepted-upstream-parity

- id: legacy-absorption-hot-water-generator-runaway
  upstream:
    path: src/idragon/dragon/hvac.py
    symbol: AbsorptionChiller.to_idf_object
    symbol_hash: sha256:17d5fb8afe2207a9772bc47b4f5424d740b3df76301f04c9155c0fbd725af969
  difference:
    upstream: connects the Chiller:Absorption hot-water generator nodes to a Boiler:HotWater demand branch that reaches runaway plant temperatures whenever a non-zero cooling load dispatches the chiller under EnergyPlus 24.2
    dotnet: preserves that legacy authoring topology for exact SimpleDragon parity, verifies all six compatibility stages at zero cooling load, and does not claim active absorption cooling or generator-energy parity
  effect:
    engineering_result: active absorption cooling is not validated and the legacy topology can terminate EnergyPlus 24.2 under non-zero cooling load
    compatibility_evidence: GRM and IDF parity, zero-cooling runtime parity, and non-zero electric-radiant heating parity remain verified
  approval: accepted-upstream-parity

- id: legacy-ashrae140-internal-source-material-severe
  upstream:
    path: null
    symbol: null
    symbol_hash: null
  difference:
    upstream: the pinned ASHRAE 140 modified GRM contains an internal-source construction layer that EnergyPlus 24.2 diagnoses as too thin or highly conductive
    dotnet: preserves the exact fixture material and requires the single normalized Severe diagnostic to match by title and count in both engines
  effect:
    engineering_result: the paired EnergyPlus run completes and its results remain comparable, but the one registered Severe diagnostic must stay visible
    diagnostic_policy: any additional Severe or Fatal diagnostic remains a compatibility failure
  approval: accepted-upstream-fixture-parity

- id: immutable-schedule-apply
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.apply
    symbol_hash: sha256:cac23120005e2cac2c4729c70471a7796840160338fff91e1c49e9670f763ba9
  difference:
    upstream: mutates the schedule by default, permits a mixed ScheduleType assignment, and treats an end date before the start date as an empty no-op range
    dotnet: returns an immutable updated schedule, requires the assigned RuleSet type to match, and rejects a reversed date range
  effect:
    engineering_result: valid same-type date ranges produce the same later-assignment-wins schedule values and Schedule:Compact output
    api_contract: mutation and invalid-input behavior intentionally follow the native immutable and strongly typed C# product interface
  approval: accepted-native-api-adaptation

- id: ruleset-scalar-minimum-upstream-attribute-error
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.element_min
    symbol_hash: sha256:33739f88089372dfdb936c28027f53c69ca8ec4ec1f47ad24ff6d8b7fc427d64
  difference:
    upstream: declares scalar support but reads other.name before dispatch and raises AttributeError for scalar operands
    dotnet: implements the declared scalar element-minimum operation for finite in-domain results, preserves RuleSet-to-RuleSet behavior, and rejects any selected non-finite or schedule-domain-invalid result during DaySchedule validation
  effect:
    engineering_result: finite scalar minima and RuleSet-to-RuleSet minima remain available for model authoring while invalid results are prevented from entering serialized schedules
    api_contract: the unusable upstream scalar branch is repaired; finite supported scalars return a RuleSet while selected non-finite or domain-invalid values raise native schedule validation instead of the unconditional upstream AttributeError
    compatibility_evidence: the exact upstream scalar AttributeError, finite repaired scalar returns, RuleSet-to-RuleSet parity, and selected non-finite and domain-invalid safety behavior remain required in a trusted cross-language receipt
  approval: accepted-upstream-bug-fix

- id: ruleset-scalar-maximum-upstream-attribute-error
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.element_max
    symbol_hash: sha256:bfffae347ffeac971d2328d400ae28e5986cc2f3c60b707e0bb55431989edd39
  difference:
    upstream: declares scalar support but reads other.name before dispatch and raises AttributeError for scalar operands
    dotnet: implements the declared scalar element-maximum operation for finite in-domain results, preserves RuleSet-to-RuleSet behavior, and rejects any selected non-finite or schedule-domain-invalid result during DaySchedule validation
  effect:
    engineering_result: finite scalar maxima and RuleSet-to-RuleSet maxima remain available for model authoring while invalid results are prevented from entering serialized schedules
    api_contract: the unusable upstream scalar branch is repaired; finite supported scalars return a RuleSet while selected non-finite or domain-invalid values raise native schedule validation instead of the unconditional upstream AttributeError
    compatibility_evidence: the exact upstream scalar AttributeError, finite repaired scalar returns, RuleSet-to-RuleSet parity, and selected non-finite and domain-invalid safety behavior remain required in a trusted cross-language receipt
  approval: accepted-upstream-bug-fix

- id: nonfinite-result-ruleset-add
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__add__
    symbol_hash: sha256:d658d7f91f8ee7dafbca0504b70bde094910f90830fc85a732e21dcab8ff2405
  difference:
    upstream: accepts a Real RuleSet when addition produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite addition result during child DaySchedule validation
  effect:
    engineering_result: every finite-result addition retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar and RuleSet behavior, fallback and override topology, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-mul
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__mul__
    symbol_hash: sha256:dfe4535f2bfc5d3e8823015e09f766c4bffb1eaa34a5d34641f5d7b86db22094
  difference:
    upstream: accepts a Real RuleSet when multiplication produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite multiplication result during child DaySchedule validation
  effect:
    engineering_result: every finite-result multiplication retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar and RuleSet behavior, fallback and override topology, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-radd
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__radd__
    symbol_hash: sha256:7d78c731949b203b143a486363e54f8572d57c8f12f0a598d7d0470ac776729e
  difference:
    upstream: accepts a Real RuleSet when reverse addition produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-addition result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse addition retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar behavior, preserved missing overrides, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-rmul
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__rmul__
    symbol_hash: sha256:7359aee63c4e4e2dc1fd2c80435b39ac3e7989f60e89e4f9951784b13c003a99
  difference:
    upstream: accepts a Real RuleSet when reverse multiplication produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-multiplication result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse multiplication retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar behavior, preserved missing overrides, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-rsub
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__rsub__
    symbol_hash: sha256:0ee38c580eba67e6c30a824516a9bf4cf97a4965f28ce732a7271ad8b705d0d6
  difference:
    upstream: accepts a Real RuleSet when reverse subtraction produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-subtraction result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse subtraction retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar behavior, preserved missing overrides, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-rtruediv
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__rtruediv__
    symbol_hash: sha256:b665fd3ac19d91fed1717628316d613dd189aa9199ad0f9a11bf44deabbbd9a0
  difference:
    upstream: accepts a Real RuleSet when reverse division produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-division result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse division retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: zero divisors retain their ordinary failure behavior, while any nonzero operation yielding a non-finite child raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar behavior, preserved missing overrides, zero-divisor behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-sub
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__sub__
    symbol_hash: sha256:d13292383b4ac45ca61e2e5a3af7f47116cecd9427402a0f82f2facc9a748e8f
  difference:
    upstream: accepts a Real RuleSet when subtraction produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite subtraction result during child DaySchedule validation
  effect:
    engineering_result: every finite-result subtraction retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar and RuleSet behavior, fallback and override topology, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-truediv
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.__truediv__
    symbol_hash: sha256:5ce5d9fa78fe66d885f07337ea82654b3976d0a954f4dbd9870d2abb08eb272e
  difference:
    upstream: accepts a Real RuleSet when division produces a non-finite binary64 value in an operated day, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite division result during child DaySchedule validation
  effect:
    engineering_result: every finite-result division retains the same weekday and weekend fallbacks, override topology, values, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: zero divisors retain their ordinary failure behavior, while any nonzero operation yielding a non-finite child raises native schedule validation instead of returning a RuleSet containing a non-finite Real DaySchedule
    compatibility_evidence: normal finite scalar and RuleSet behavior, fallback and override topology, zero-divisor behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-ruleset-normalize-by-max
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.normalize_by_max
    symbol_hash: sha256:92c2f28741585003d7e2bab24c4bff10cd1fa42133eb1cf6870409b37ec6ba55
  difference:
    upstream: returns a new RuleSet that independently normalizes every present day slot by that DaySchedule maximum, preserves missing overrides, accepts a Real child whose finite inputs normalize to infinity, permits an empty or whitespace-only explicit result name, and preserves surrounding name whitespace
    dotnet: returns the same immutable slot-by-slot normalized shape for finite results, rejects a non-finite child during DaySchedule validation, rejects explicit names that cannot form valid EnergyPlus object identifiers, and trims surrounding whitespace from otherwise valid names
  effect:
    engineering_result: finite normalized values, per-slot scaling, result types, valid default and explicit names, and override topology remain equivalent while normalization overflow and invalid EnergyPlus names are prevented from entering authoring output
    api_contract: a non-finite normalized child raises native schedule validation instead of returning a RuleSet containing infinity; empty or whitespace-only explicit names raise native name validation and surrounding whitespace is normalized
    compatibility_evidence: independent weekday, weekend, and override scaling, zero-maximum behavior, missing-override preservation, source immutability, names and types, explicit finite-input normalization overflow, invalid explicit-name rejection, and surrounding-whitespace normalization remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: deterministic-ruleset-where-day-names
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: RuleSet.where
    symbol_hash: sha256:b245f2e84cd0e4b15b7f03d663409c07792e05f5143a8aae6a1e567769fa726a
  difference:
    upstream: assigns the requested or default RuleSet name but omits every child DaySchedule.where name, producing process-specific hexadecimal child names; it also permits a whitespace-only explicit result name and preserves surrounding name whitespace, unsupported Python operands follow Python-native validation and evaluation order, and a selected non-finite scalar can enter a Real result child
    dotnet: assigns stable slot-qualified child names, rejects whitespace-only names that cannot form valid EnergyPlus object identifiers, trims surrounding whitespace from otherwise valid names before building child names, validates unsupported CLR operand kinds with the native strongly typed contract and ordering, and rejects selected non-finite child values under the finite-only engineering schedule invariant
  effect:
    engineering_result: for supported finite scalar, DaySchedule, and RuleSet branches with valid result names, selected values, inferred or explicit result type, weekday and weekend fallback behavior, and override topology remain equivalent; selected non-finite results and invalid EnergyPlus names are prevented from entering authoring output
    api_contract: invalid CLR operands and whitespace-only explicit names use native validation rather than reproducing Python acceptance, exception text, or evaluation order; surrounding name whitespace is normalized consistently across the result and its children
    raw_idf_text: generated child schedule names are deterministic and can differ from the upstream process-address names
    compatibility_evidence: every supported finite branch shape, condition-only and branch-only overrides, weekday and weekend fallback, inferred and explicit types, valid explicit, empty-defaulted, omitted, rejected whitespace-only, and normalized surrounding-whitespace result names, selected non-finite rejection, invalid operands, and deterministic slot-qualified child names remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-add
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__add__
    symbol_hash: sha256:b1f53fbc391503bdfb118688bb177fa95bcc24c9dfa53fed27cf00442c23eb72
  difference:
    upstream: accepts a Real Schedule when addition produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite addition result during child DaySchedule validation
  effect:
    engineering_result: every finite-result addition retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning an annual Schedule containing a non-finite Real value
    compatibility_evidence: finite scalar and Schedule behavior, asymmetric compact boundaries, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-mul
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__mul__
    symbol_hash: sha256:341d9b28a235a5361ed9d16141e7ffdeac6f8933c1e420208fefb4714298029a
  difference:
    upstream: accepts a Real Schedule when multiplication produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite multiplication result during child DaySchedule validation
  effect:
    engineering_result: every finite-result multiplication retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning an annual Schedule containing a non-finite Real value
    compatibility_evidence: finite scalar and Schedule behavior, asymmetric compact boundaries, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-radd
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__radd__
    symbol_hash: sha256:ebaafbe81f9daa483e5f13afbd779c49f7308ef8b9551e10dac213d64a37c045
  difference:
    upstream: accepts a Real Schedule when reverse addition produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-addition result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse addition retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning an annual Schedule containing a non-finite Real value
    compatibility_evidence: finite scalar behavior, reverse-operation names, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-rmul
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__rmul__
    symbol_hash: sha256:279533a07d8189cc0a3f7fa57174faab7ea8500caf007ed8fc08ddb067353be2
  difference:
    upstream: accepts a Real Schedule when reverse multiplication produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-multiplication result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse multiplication retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning an annual Schedule containing a non-finite Real value
    compatibility_evidence: finite scalar behavior, upstream multiplication-order naming, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-rsub
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__rsub__
    symbol_hash: sha256:e84f78d3b0d4f00c202d644a04d90915a5302c87e28b1b37394c9504a4400047
  difference:
    upstream: accepts a Real Schedule when reverse subtraction produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-subtraction result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse subtraction retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning an annual Schedule containing a non-finite Real value
    compatibility_evidence: finite scalar behavior, reverse-operation names, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-rtruediv
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__rtruediv__
    symbol_hash: sha256:32d900f7d3189a35816962c0dae8c984f77bf619f9d75e74464a20803b090209
  difference:
    upstream: accepts a Real Schedule when reverse division produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-division result during child DaySchedule validation
  effect:
    engineering_result: every finite-result reverse division retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: zero divisors retain ordinary failure behavior, while any nonzero operation yielding a non-finite child raises native schedule validation instead of returning a non-finite annual Schedule
    compatibility_evidence: finite scalar behavior, reverse-operation names, zero-divisor behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-sub
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__sub__
    symbol_hash: sha256:c963a4baf0da27e3807668ca2a93d212c429e167beebb78cdacce21be1c935dc
  difference:
    upstream: accepts a Real Schedule when subtraction produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite subtraction result during child DaySchedule validation
  effect:
    engineering_result: every finite-result subtraction retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: a non-finite child result raises native schedule validation instead of returning an annual Schedule containing a non-finite Real value
    compatibility_evidence: finite scalar and Schedule behavior, asymmetric compact boundaries, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-truediv
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.__truediv__
    symbol_hash: sha256:cb9dd7d8cd8f71bb8ddff07c59959540a9a259b1caf6268128a68e17e375652c
  difference:
    upstream: accepts a Real Schedule when division produces a non-finite binary64 value in any operated RuleSet, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite division result during child DaySchedule validation
  effect:
    engineering_result: every finite-result division retains the same annual boundaries, RuleSet topology, values, names, and result type while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: zero divisors retain ordinary failure behavior, while any nonzero operation yielding a non-finite child raises native schedule validation instead of returning a non-finite annual Schedule
    compatibility_evidence: finite scalar and Schedule behavior, asymmetric compact boundaries, zero-divisor behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-schedule-normalize-by-max
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.normalize_by_max
    symbol_hash: sha256:b12e2905f36794820228b307d1ee4dacf368b1c00e19a26b7423acae87bab5d3
  difference:
    upstream: returns a new compact Schedule that independently normalizes every present RuleSet, accepts a Real child whose finite inputs normalize to infinity, permits empty or whitespace-only explicit result names, and preserves surrounding name whitespace
    dotnet: returns the same immutable compact annual shape for finite results, rejects a non-finite child during DaySchedule validation, rejects names that cannot form valid EnergyPlus object identifiers, and trims surrounding whitespace from otherwise valid names
  effect:
    engineering_result: finite normalized values, annual boundaries, child topology, result types, and valid names remain equivalent while normalization overflow and invalid EnergyPlus names are prevented from entering authoring output
    api_contract: a non-finite normalized child raises native schedule validation; empty or whitespace-only explicit names raise native name validation and surrounding whitespace is normalized
    compatibility_evidence: per-child scaling, zero-maximum behavior, compact topology, source immutability, names and types, explicit normalization overflow, invalid-name rejection, and surrounding-whitespace normalization remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: deterministic-schedule-where-child-names
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: Schedule.where
    symbol_hash: sha256:d673aaaebf6468cbce8fe25610252702146eda0a155bef637940e26305108315
  difference:
    upstream: assigns the requested or default outer Schedule name and lets each nested RuleSet.where use its literal WHERE default, but omits every nested DaySchedule name and therefore produces process-specific hexadecimal DaySchedule names; it permits whitespace-only names and preserves surrounding whitespace, follows Python-native invalid-operand evaluation and diagnostics, and can return a selected non-finite Real value
    dotnet: preserves the nested WHERE RuleSet name and assigns stable slot-qualified DaySchedule names, rejects whitespace-only names that cannot form valid EnergyPlus identifiers, trims surrounding name whitespace, follows native strongly typed CLR validation, and rejects selected non-finite child values while preserving unselected non-finite branch behavior
  effect:
    engineering_result: for supported finite scalar, DaySchedule, RuleSet, and Schedule branches with valid names, selected values, inferred or explicit types, fallback sources, override topology, and asymmetric annual boundaries remain equivalent; selected non-finite results and invalid names are prevented from entering authoring output
    api_contract: invalid CLR operands and names use native validation instead of Python exception text or ordering, and arbitrarily large integer branches retain eager conversion failure even when unselected
    raw_idf_text: nested DaySchedule names are deterministic and can differ from upstream process-address names
    compatibility_evidence: every supported branch shape, scalar kind, annual boundary combination, inferred and explicit type, omitted and empty default names, rejected whitespace-only and trimmed surrounding-whitespace names, eager huge-integer failure, selected and unselected non-finite behavior, source immutability, and deterministic nested names remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: construction-equality-native-null-operand
  upstream:
    path: src/idragon/dragon/construction.py
    symbol: Construction.__eq__
    symbol_hash: sha256:8bf568b5f76ed813063ea04fd2eedf087e8f2525c2be9b9febbdb150a906b019
  difference:
    upstream: directly reads the operand name and layers, so duck-typed operands may compare while null or incompatible operands can raise AttributeError
    dotnet: matches the same supported name and ordered-layer values only for the CLR Construction type and follows native .NET equality by returning false for null or unrelated operands
  effect:
    engineering_result: none for valid typed constructions used in model authoring and EnergyPlus export
    api_contract: invalid-operand equality follows native .NET false semantics instead of preserving the Python AttributeError
    compatibility_evidence: same-type value parity and the exact null-operand difference remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: construction-hash-native-runtime-value
  upstream:
    path: src/idragon/dragon/construction.py
    symbol: Construction.__hash__
    symbol_hash: sha256:5994dd14a598a335d7945a1e39b59d93fd6bed9afbaff1308019b57bf22d0889
  difference:
    upstream: returns the Python 3.12 hash of name as a Python-sized integer whose value depends on PYTHONHASHSEED and the Python runtime
    dotnet: returns the native .NET Int32 ordinal string hash of Name and preserves the same name-only dependency without claiming the same numeric value
  effect:
    engineering_result: none because hash codes do not participate in EnergyPlus model values or serialized engineering results
    api_contract: hash codes are runtime-local and must not be persisted or compared numerically across Python and .NET
    compatibility_evidence: name-only dependency and same-name invariants remain required while exact cross-runtime numeric equality is explicitly excluded
  approval: accepted-native-api-adaptation

- id: layer-equality-native-null-operand
  upstream:
    path: src/idragon/dragon/construction.py
    symbol: Layer.__eq__
    symbol_hash: sha256:b3fd4452af62f2d402279427187e70e6165f14be9a0b0543f8702dee39a473e6
  difference:
    upstream: directly reads the operand material and thickness while ignoring name, so duck-typed operands may compare while null or incompatible operands can raise AttributeError
    dotnet: matches the same supported material and thickness values only for the CLR Layer type while ignoring Name and follows native .NET equality by returning false for null or unrelated operands
  effect:
    engineering_result: none for valid typed layers used in constructions and EnergyPlus export
    api_contract: invalid-operand equality follows native .NET false semantics instead of preserving the Python AttributeError
    compatibility_evidence: same-type value parity and the exact null-operand difference remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: layer-hash-native-runtime-value
  upstream:
    path: src/idragon/dragon/construction.py
    symbol: Layer.__hash__
    symbol_hash: sha256:5994dd14a598a335d7945a1e39b59d93fd6bed9afbaff1308019b57bf22d0889
  difference:
    upstream: returns the Python 3.12 hash of name as a Python-sized integer whose value depends on PYTHONHASHSEED and the Python runtime
    dotnet: returns the native .NET Int32 ordinal string hash of each Name and preserves the same name-only dependency without claiming collision-dependent distinct numeric values
  effect:
    engineering_result: none because hash codes do not participate in EnergyPlus layer values or serialized engineering results
    api_contract: hash codes are runtime-local and must not be persisted or compared numerically across Python and .NET
    compatibility_evidence: equal-different-name equality and the name-only hash dependency of each object remain required, while the pinned Python fixture alone records distinct hashes and exact cross-runtime numeric equality is explicitly excluded
  approval: accepted-native-api-adaptation

- id: material-equality-native-null-operand
  upstream:
    path: src/idragon/dragon/construction.py
    symbol: Material.__eq__
    symbol_hash: sha256:6ef680a2e300bcb56672f0d036de1b4aea3630cb90a635a3e45002ff8535dbf5
  difference:
    upstream: matches same-type Material name conductivity density and specific-heat values while ignoring optical properties and roughness but raises TypeError for null or non-Material operands
    dotnet: matches the same supported properties and follows native .NET equality by returning false for null or unrelated operands
  effect:
    engineering_result: none for valid typed materials used in constructions and EnergyPlus export
    api_contract: invalid-operand equality follows native .NET false semantics instead of preserving the Python TypeError
    compatibility_evidence: same-type value parity and the exact null-operand difference remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: schedule-type-validation-native-safety-adaptation
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: ScheduleType.validate
    symbol_hash: sha256:b09903103bf95c771eb228f80666fb264e176204c332873795c2d96f86056bcb
  difference:
    upstream: accepts NaN and positive or negative infinity for Real schedules and returns Python integers for successful OnOff validation
    dotnet: rejects non-finite values in the typed ScheduleType and DaySchedule authoring path and normalizes successful strongly typed numeric results to CLR double
  effect:
    engineering_result: finite typed schedule values retain the pinned behavior, while non-finite values are prevented from entering an IDF through the typed schedule path
    api_contract: Real non-finite inputs follow the native engineering safety invariant and OnOff results follow the strongly typed CLR double contract
    compatibility_evidence: all finite, boundary, type, and domain cases plus the three Real safety divergences and four OnOff numeric-kind adaptations remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-add
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__add__
    symbol_hash: sha256:f2cc675e8c909fae4fa4461fb915249045e55b0e6d7b754575b00ba2cecf7610
  difference:
    upstream: accepts a Real schedule when addition produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite addition result during DaySchedule validation
  effect:
    engineering_result: every finite-result addition remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-radd
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__radd__
    symbol_hash: sha256:5a5ededeac5428a72339d7725836d9062c10a107cbc821e2659160c73668831f
  difference:
    upstream: accepts a Real schedule when reverse addition produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-addition result during DaySchedule validation
  effect:
    engineering_result: every finite-result reverse addition remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-sub
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__sub__
    symbol_hash: sha256:55fed2bd2b5cbb9b3ed69e4e8c1da4207d382e78ede3fb674750e395f4a1c4e8
  difference:
    upstream: accepts a Real schedule when subtraction produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite subtraction result during DaySchedule validation
  effect:
    engineering_result: every finite-result subtraction remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-rsub
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__rsub__
    symbol_hash: sha256:a1fa02e18d86596b88fdebeceedaa48459b6e1068c301c93ba26170f41c37418
  difference:
    upstream: accepts a Real schedule when reverse subtraction produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-subtraction result during DaySchedule validation
  effect:
    engineering_result: every finite-result reverse subtraction remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-mul
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__mul__
    symbol_hash: sha256:c8bbdbc48d7e465d159ab6b829609d582004ead56657b7475b7caeb552454aea
  difference:
    upstream: accepts a Real schedule when multiplication produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite multiplication result during DaySchedule validation
  effect:
    engineering_result: every finite-result multiplication remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-rmul
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__rmul__
    symbol_hash: sha256:87f6bef2e0be21121fdc990138093d2d07cc225d5edaff5d2129660a902a4e7f
  difference:
    upstream: accepts a Real schedule when reverse multiplication produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-multiplication result during DaySchedule validation
  effect:
    engineering_result: every finite-result reverse multiplication remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-truediv
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__truediv__
    symbol_hash: sha256:d4bf77a6d67c06dfa3076336eac461f80855fdbfb2f72d46115cd8e67c10ca0b
  difference:
    upstream: accepts a Real schedule when division produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite division result during DaySchedule validation
  effect:
    engineering_result: every finite-result division remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-rtruediv
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.__rtruediv__
    symbol_hash: sha256:9bc405fae0ca82d5a0ab953af9197871e5c71248a267d07d408071d44abbb374
  difference:
    upstream: accepts a Real schedule when reverse division produces a non-finite binary64 result, whether from finite overflow or a non-finite scalar operand
    dotnet: preserves the finite-only engineering schedule invariant and rejects every non-finite reverse-division result during DaySchedule validation
  effect:
    engineering_result: every finite-result reverse division remains equivalent while an overflowing result is prevented from entering EnergyPlus authoring output
    api_contract: CLR Boolean, integral, and floating-point operands form the native strongly typed operator surface; Python arbitrary-precision integer call syntax is not exposed; a non-finite result raises native schedule validation instead of returning a non-finite Real schedule
    compatibility_evidence: normal CLR primitive behavior, finite-input overflow, and a non-finite scalar-result divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-element-min
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.element_min
    symbol_hash: sha256:ac3a8af2147d4a6fb6c85812769b0eea7ddf2c96342d28a0b72c659b0ed1623c
  difference:
    upstream: accepts a Real schedule when element-wise minimum selects a non-finite floating-point scalar
    dotnet: preserves the finite-only engineering schedule invariant and rejects the selected non-finite minimum result during DaySchedule validation
  effect:
    engineering_result: finite scalar and schedule minima remain equivalent while a selected non-finite result is prevented from entering EnergyPlus authoring output
    api_contract: a selected non-finite minimum raises a native schedule validation exception instead of returning a non-finite Real schedule
    compatibility_evidence: normal finite behavior and an explicit selected non-finite scalar divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: nonfinite-result-day-schedule-element-max
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.element_max
    symbol_hash: sha256:6bf704e3d166ef0957b56ff7cd2b0841a32b80e8c1783e12cd698a183cb20f05
  difference:
    upstream: accepts a Real schedule when element-wise maximum selects a non-finite floating-point scalar
    dotnet: preserves the finite-only engineering schedule invariant and rejects the selected non-finite maximum result during DaySchedule validation
  effect:
    engineering_result: finite scalar and schedule maxima remain equivalent while a selected non-finite result is prevented from entering EnergyPlus authoring output
    api_contract: a selected non-finite maximum raises a native schedule validation exception instead of returning a non-finite Real schedule
    compatibility_evidence: normal finite behavior and an explicit selected non-finite scalar divergence remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: immutable-day-schedule-normalize-by-max
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.normalize_by_max
    symbol_hash: sha256:dd857df94e8e53388add91cb81cb88d6e8de762ee49553fdd52f37979f5259c7
  difference:
    upstream: supports an inplace mode that mutates the existing data list, returns None, preserves the original type and unit, and bypasses schedule-domain validation; finite Real inputs can normalize to infinity and remain accepted
    dotnet: follows the native immutable schedule model and always returns a newly validated DaySchedule while leaving the source unchanged; the finite-only engineering invariant rejects a non-finite normalized result
  effect:
    engineering_result: finite-result default copy-mode values, result type, result name, and cleared result unit remain equivalent while a non-finite result is prevented from entering EnergyPlus authoring output
    api_contract: callers replace their schedule reference with the returned immutable value instead of requesting in-place mutation, and normalization overflow raises native schedule validation instead of returning infinity
    compatibility_evidence: copy-mode parity, source immutability, the exact in-place mutation and None-return difference, and explicit finite-input normalization overflow remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation

- id: deterministic-day-schedule-where-name
  upstream:
    path: src/idragon/dragon/profile.py
    symbol: DaySchedule.where
    symbol_hash: sha256:33c2a95572c296a03947b50bb90895168c069b0f054bac875f4039bb8232595c
  difference:
    upstream: when name is omitted, assigns the process-specific hexadecimal memory address of the newly created schedule; unsupported Python operands raise Python-native TypeError diagnostics in true-then-false evaluation order; a selected non-finite scalar can enter a Real result schedule
    dotnet: when name is omitted, assigns the stable native product name WHERE; unsupported CLR operand kinds use native ScheduleOperationException validation and overload ordering; selected non-finite values are rejected by the finite-only engineering schedule invariant
  effect:
    engineering_result: for supported finite integer, floating-point, and DaySchedule operands, selected values, result type, and cleared result unit remain equivalent; a selected non-finite result is prevented from entering EnergyPlus authoring output
    api_contract: invalid CLR operand diagnostics follow the native strongly typed API rather than reproducing Python TypeError text or evaluation order
    raw_idf_text: an omitted result name is deterministic and can differ from the Python process-address name
    compatibility_evidence: every supported finite scalar and schedule branch shape, explicit-name behavior, selected non-finite rejection, invalid operand behavior, and the normalized default-name policy difference remain required in a trusted cross-language receipt
  approval: accepted-native-api-adaptation
