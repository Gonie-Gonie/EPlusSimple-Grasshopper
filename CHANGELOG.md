# Changelog

All notable changes to this project are documented here. The project uses
independent `invisible-dragon-v*` and `simple-dragon-v*` release tags.

## Unreleased

### Added

- Add two independently installable, Gonie-Gonie-owned Grasshopper products:
  InvisibleDragon, the C# port of the historical IDragon layer, and
  SimpleDragon, the C# port of the historical EPlusSimple layer.
- Support Rhino 7+ with a Rhino 7 `net48` payload and Rhino 8
  `net7.0-windows` / `net8.0-windows` payloads.
- Port deterministic materials, constructions, profiles, planar geometry,
  zones, IDD/IDF compilation, model conversion, and persisted Grasshopper
  domain values.
- Add full authoring paths for source and zone-side HVAC systems, cooling
  towers, ERVs, PV, direct Zone ownership, and SimpleDragon-to-InvisibleDragon
  conversion.
- Add composable `SD Opening`, `SD Surface`, `SD Zone`, and `SD Model` authoring
  values so opening/Surface/HVAC/ERV ownership is expressed by local wires
  without Zone/Face indices, assignment passes, or repeated model-catalog
  connections.
- Add direct `Run SimpleDragon` execution: a GRM enters and a GRR returns while
  Address/Vintage weather selection, InvisibleDragon conversion, IDF generation,
  EnergyPlus, IDD, EPW, runtime-cache, and temporary paths remain internal.
- Use direct Window/Door -> Surface -> Zone -> Model ownership wires in
  InvisibleDragon and Opening -> Surface -> Zone -> Model wires in SimpleDragon,
  with HVAC/ERV values connected directly to their owning Zone.
- Require each SimpleDragon Opening to own its Fenestration Construction and
  each SimpleDragon Surface to own its face geometry, opaque Construction,
  Boundary Intent, and Openings. Remove the direct Zone Brep and all duplicate
  Zone-level surface/opening defaults, and reject unannotated inner loops instead
  of inventing metadata.
- Represent opaque construction layers and managed simulation cases as typed
  values, so no authoring relationship depends on matching positions in two lists.
- Add `Compile InvisibleDragon`, a visible path-free EnergyPlus 24.2 compiler
  for standalone low-level InvisibleDragon model authoring.
- Add managed EnergyPlus 24.2 runtime preparation and simulation components,
  deterministic result/CSV handling, plots, DataTrees, and parallel batch
  research workflows. InvisibleDragon candidates carry the exact pinned
  EnergyPlus archive, while SimpleDragon candidates carry the exact pinned
  KoreanTMY archive and resolve addresses to cached executable EPW paths.
- Add eight genuine InvisibleDragon and SimpleDragon `.gh` definitions plus
  two named-Surface `.3dm` building models, generated by Rhino 7 and solved,
  saved, reopened, and round-trip checked in both Rhino 7 and Rhino 8.
- Separate the complex two-zone model-authoring example from the stable
  electric-radiator example used for the full managed
  `SD Model -> Run SimpleDragon -> GRR -> CSV` gate.
- Add independently branded Dragon icons, Yak archives, portable plugin ZIPs,
  package manifests, compatibility reports, and SHA-256 inventories.
- Add distinct illustration-derived icons for every public component and
  persistent parameter instead of sharing Grasshopper's default data icon.

### Development and verification

- Consolidate setup, build, reference, package, install, example, smoke,
  release, cleanup, icon, and upstream workflows behind the single
  `dev.cmd <command>` development entry point.
- Pin the historical EPlusSimple and IDragon 0.7.0 source commit for provenance
  and deterministic Python-oracle compatibility testing.
- Add reproducible `dev.cmd setup`, `dev.cmd build`, `dev.cmd reference`, and
  `dev.cmd package`
  entry points with local toolchains and disposable work routed below `.tools`
  and `temp` respectively.
- Add `dev.cmd release`, a clean/pushed-`main` local candidate gate that verifies
  the Python oracle, all production targets and tests, real EnergyPlus runs,
  tracked Grasshopper examples, package contents, and all six Rhino 7/Rhino 8
  portable-package load scenarios.

### Distribution status

- Public binaries, release tags, GitHub releases, and Yak publication remain
  withheld pending review of the historical upstream standalone-license
  omission recorded in `NOTICE.md`. `dev.cmd release` creates only a local
  verified candidate and performs none of those publication actions.
